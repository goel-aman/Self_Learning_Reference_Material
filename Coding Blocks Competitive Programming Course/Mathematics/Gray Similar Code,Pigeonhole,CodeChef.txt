#include<iostream>
#define l long long int;

using namespace std;

int main(){
	unsigned ll n;
	vector<unsigned ll> v;
	cin>>n;
	for(ll i=0;i<n;i++){
		unsigned ll no;
		cin>>no;
		if(n < 130){
			v.push_back(a);
		}
	}
	
	// Brute Force for small values
	// 4 nested loop to pick 4 number
	for(int i=0;i<n;i++){
		for(int j=i+1;j<n;j++){
			for(int k=j+1;k<n;k++){
				for(int l=k+1;l<n;l++){
					if((v[i]^v[j]^v[k]^v[l]) == 0){
						cout<<"YES";
						return 0;
					}
				}
			}
		}
	}
	return 0;
}